package provider.wz.binary.serialize;

import provider.wz.binary.WzImage;

public final class WzSound extends WzSerialize {
    public WzSound(WzImage parent, int offset) {
        super(parent, offset);
    }
}
