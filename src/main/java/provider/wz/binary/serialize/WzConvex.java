package provider.wz.binary.serialize;

import provider.wz.binary.WzImage;

public final class WzConvex extends WzSerialize {
    public WzConvex(WzImage parent, int offset) {
        super(parent, offset);
    }
}
