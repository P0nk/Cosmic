apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cosmic.fullname" . }}-test-connection"
  labels:
    {{- include "cosmic.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cosmic.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
